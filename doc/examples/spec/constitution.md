## 1 Purpose & Scope

This project serves as a template on how to build microservices.,
It is based on these core technologies
- spring boot 4.0 as the underlying framework
- build.gradle.kts for the build process


## 2 Build process
- the build is based on gradle build.gradle.kts
- the gradle version should be at least 9.2.1
- new projects should be based on latest stable spring boot 4.x
- the task "jibJvmImage" will build and push a jvm multi-arch image via jib
- spring data jpa + h2 database is used for persistence

#### 3 layered architecture
- the architecture is strictly seperated by layers, seperated by packages
- controller, contains the rest controller
- controller/dto, contains the DTOs
- logic, contains the business logic
- logic/mapper, contains the mapper generated by mapstruct, that maps entities to DTOs
- persistence, contains the repositories
- persistence/entity contains the JPA entities
- persistence is backed up by flyway to create the database schema, the file can be found inside resources/db
- adapter contains outbound http adapters, implemented by spring boot 4 declarative http client
- these adapters are also annotated with @CircuitBreaker annotation to allow for resilient ciruit breaking
